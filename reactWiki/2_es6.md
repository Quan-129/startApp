# Kiến thức JavaScript (ES6+) Cốt lõi cho React ⚛️

Để học React hiệu quả, bạn không cần biết hết mọi thứ về JS, nhưng **BẮT BUỘC** phải nắm vững 8 kỹ thuật dưới đây.

---

## 1. Khai báo biến: `let` và `const`
Trong React, chúng ta kiểm soát chặt chẽ việc thay đổi dữ liệu. Quên `var` đi.

* **`const`**: Dùng mặc định (95%). Dùng cho các giá trị không định gán lại (Component, Function, Config).
* **`let`**: Chỉ dùng khi biết chắc chắn biến đó sẽ thay đổi giá trị (trong vòng lặp, logic toggle).

```javascript
// ❌ Cũ (Không nên dùng)
var name = "Gemini";

// ✅ Mới
const API_URL = "[https://api.example.com](https://api.example.com)"; // Hằng số
let count = 0;   // Biến đếm (sẽ thay đổi)

count = count + 1; // OK
// API_URL = "abc"; // ❌ Lỗi ngay lập tức vì const không được gán lại
```

---

## 2. Arrow Function (Hàm mũi tên)
Cú pháp ngắn gọn, xử lý `this` tốt hơn. Đây là chuẩn viết Component hiện đại.

```javascript
// ❌ Hàm thường (Function Declaration)
function sum(a, b) {
  return a + b;
}

// ✅ Arrow Function
const sum = (a, b) => {
  return a + b;
};

// 🔥 Siêu ngắn (Implicit Return - dùng khi chỉ có 1 dòng trả về)
const sum = (a, b) => a + b;
```

---

## 3. Destructuring (Bóc tách dữ liệu)
Kỹ thuật lấy dữ liệu từ Object/Array cực nhanh. Dùng rất nhiều trong **Props** và **State**.

### Với Object (Thường dùng cho Props)
```javascript
const user = { name: "Nam", age: 20, city: "HCM" };

// ❌ Cách cũ
const name = user.name;
const age = user.age;

// ✅ Destructuring
const { name, age } = user; 
console.log(name); // "Nam"
```

### Với Array (Thường dùng cho useState)
```javascript
const colors = ["red", "blue"];
const [firstColor, secondColor] = colors;
// firstColor = "red"
```

---

## 4. Spread Operator `...` (Toán tử rải)
Dùng để sao chép hoặc gộp mảng/object. **Rất quan trọng** để đảm bảo tính bất biến (Immutability) trong React (không sửa trực tiếp state cũ).

```javascript
const oldArray = [1, 2, 3];

// ❌ Không dùng push (làm thay đổi mảng gốc)
// oldArray.push(4); 

// ✅ Tạo mảng mới, copy mảng cũ và thêm số 4
const newArray = [...oldArray, 4]; 
// Kết quả: [1, 2, 3, 4]

// --- Với Object ---
const user = { name: "Nam", role: "User" };
const admin = { ...user, role: "Admin" }; // Copy user, ghi đè role
```

---

## 5. Array Methods: `map()` và `filter()`
React không dùng vòng lặp `for` để render giao diện, mà dùng `map()`.

### `map()`: Biến đổi mảng (Render danh sách)
```javascript
const nums = [1, 2, 3];
const doubleNums = nums.map((n) => n * 2); 
// Kết quả: [2, 4, 6]

// Trong React:
// data.map(item => <div key={item.id}>{item.name}</div>)
```

### `filter()`: Lọc mảng (Chức năng xóa/tìm kiếm)
```javascript
const products = [
  { id: 1, name: "Iphone" },
  { id: 2, name: "Samsung" }
];

// Lọc lấy những thằng KHÔNG phải Iphone (tương đương xóa Iphone)
const newProducts = products.filter(p => p.name !== "Iphone");
```

---

## 6. Template Literals (Chuỗi mẫu)
Dùng dấu huyền (Backticks `` ` ``) để chèn biến vào chuỗi dễ dàng.

```javascript
const name = "Nam";

// ❌ Cũ
console.log("Hello " + name + "!");

// ✅ Mới
console.log(`Hello ${name}!`); 
```

---

## 7. Modules (Import / Export)
Chia code thành nhiều file nhỏ để dễ quản lý.

* **Export Default:** Mỗi file chỉ xuất 1 cái chính.
    ```javascript
    // Component.js
    const App = () => { ... }
    export default App;

    // main.js
    import App from './Component'; // Tên gì cũng được
    ```

* **Named Export:** Xuất nhiều cái trong 1 file.
    ```javascript
    // utils.js
    export const PI = 3.14;
    export const sum = (a, b) => a + b;

    // main.js
    import { PI, sum } from './utils'; // Phải đúng tên
    ```

---

## 8. Optional Chaining `?.`
Giúp truy cập property lồng nhau an toàn mà không bị crash app nếu dữ liệu null/undefined.

```javascript
const user = {}; // Dữ liệu chưa tải xong

// ❌ Lỗi: Cannot read properties of undefined
// console.log(user.address.street); 

// ✅ An toàn: Trả về undefined nếu address không tồn tại
console.log(user?.address?.street); 
```

---

## 🎯 Bài tập thực hành
Viết lại hàm sau sử dụng **Arrow Function**, **Destructuring** và **Template String**:

```javascript
// Code cũ cần tối ưu
function showInfo(user) {
    var name = user.name;
    var job = user.job;
    return "User: " + name + " is working as " + job;
}
```

### Đáp án tham khảo:
```javascript
const showInfo = ({ name, job }) => `User: ${name} is working as ${job}`;
```